<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ChoiceMapper">

    <sql id="Base_Column_List">
        id,teacher_id,student_id,course_id
    </sql>

    <!-- 课程字段必须逐列列出，不能用 course.*：
         course 与 choice 都有 id/teacher_id 列，列标签重复时自动映射按「后者覆盖」生效，
         Choice.id 会拿到课程 id 而不是选课记录 id，退课就会按错误的 id 删除。 -->
    <select id="selectAll" resultType="com.example.entity.Choice">
        select choice.id, choice.teacher_id, choice.student_id, choice.course_id,
               teacher.name as teacherName, student.name as studentName,
               course.name as name, course.type as type, course.score as score, course.num as num,
               course.room as room, course.week as week, course.segment as segment, course.status as status
        from choice
        left join teacher on choice.teacher_id = teacher.id
        left join student on choice.student_id = student.id
        left join course on choice.course_id = course.id
        <where>
            <if test="id != null"> and choice.id= #{id}</if>
            <!-- 按课程名搜索：choice 表本身没有 name 列，条件必须落在 course 上 -->
            <if test="name != null"> and course.name like concat('%', #{name}, '%')</if>
            <if test="teacherId != null"> and choice.teacher_id = #{teacherId}</if>
            <if test="studentId != null"> and choice.student_id = #{studentId}</if>
            <if test="courseId != null"> and choice.course_id = #{courseId}</if>
        </where>
        order by choice.id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Choice">
        select
        <include refid="Base_Column_List" />
        from choice
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from choice
        where  id = #{id}
    </delete>

    <delete id="deleteBatchIds">
        delete from choice
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <insert id="insert" parameterType="com.example.entity.Choice" useGeneratedKeys="true">
        insert into choice
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="teacherId != null">teacher_id,</if>
            <if test="studentId != null">student_id,</if>
            <if test="courseId != null">course_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="teacherId != null">#{teacherId},</if>
            <if test="studentId != null">#{studentId},</if>
            <if test="courseId != null">#{courseId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Choice">
        update choice
        <set>
            <if test="teacherId != null">
                teacher_id = #{teacherId},
            </if>
            <if test="studentId != null">
                student_id = #{studentId},
            </if>
            <if test="courseId != null">
                course_id = #{courseId},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>