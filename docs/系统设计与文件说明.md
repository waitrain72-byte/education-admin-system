# 教务管理系统 · 系统流程与文件说明

> 适用版本：Spring Boot 2.7.18 + Vue 3（Element Plus）+ uni-app（Vue 3）+ MySQL 5.7/8.x
> 配套文档：[数据库 ER 图](./数据库ER图.md)

---

## 一、系统概述与整体架构

系统为前后端分离的教育管理平台，覆盖管理员 / 教师 / 学生三类角色，包含三端交付：

| 端           | 目录                                    | 技术                                                             | 说明               |
| ------------ | --------------------------------------- | ---------------------------------------------------------------- | ------------------ |
| Web 管理端   | `vue/`                                  | Vue 3 + TypeScript + Element Plus + Pinia + vue-i18n + Vite      | 功能最全的管理后台 |
| 后端服务     | `springboot/`                           | Spring Boot 2.7 + MyBatis + PageHelper + MySQL + JWT + WebSocket | 统一 API 服务      |
| App 小程序端 | `BISHE-mobile/`（独立仓库 mobile 分支） | uni-app（Vue 3）编译到微信小程序                                 | 与 Web 端功能对齐  |

```
┌─────────────────┐   ┌─────────────────┐   ┌──────────────────┐
│  Web 管理端      │   │  App 小程序端    │   │  MySQL 数据库     │
│  vue (8080/dev)  │   │  BISHE-mobile   │   │  xm_educational  │
└────────┬────────┘   └────────┬────────┘   │  _manager        │
         │ /api 代理            │ 直连 9091   └────────▲─────────┘
         ▼                     ▼                      │
┌─────────────────────────────────────────────┐        │
│  Spring Boot 后端 (9091)                     ├────────┘
│  JwtInterceptor → PermissionAspect           │
│  → Controller → Service → Mapper(XML)        │
│         ↓ WebSocket ↓ 定时任务 ↓ AOP 日志     │
└─────────────────────────────────────────────┘
```

- 开发模式：Web 前端 Vite 将 `/api` 前缀代理到 `localhost:9091`（去除前缀）；小程序经 `src/utils/config.js` 的 baseUrl 直连。
- 生产模式：nginx 托管前端静态资源并将 `/api` 反代到后端；数据库口令、JWT 密钥、CORS 来源均支持环境变量注入。

---

## 二、后端文件地图（springboot/src/main/java/com/example/）

### 2.1 启动与横切关注点

| 文件                                                                 | 职责                                                                                                        |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `SpringbootApplication.java`                                         | Spring Boot 启动类                                                                                          |
| `common/annotation/RequirePermission`                                | 权限注解：类上声明模块（按请求方式推导 view/manage），方法上声明完整权限码                                  |
| `common/annotation/NoRepeatSubmit`                                   | 防重复提交注解：关键写接口 2 秒内拒绝重复请求                                                               |
| `common/config/JwtInterceptor`                                       | 登录拦截器：校验 JWT、解析当前用户；放行 /login、/captcha、/files/\*\* 等白名单                             |
| `common/config/PermissionAspect`                                     | RBAC 切面：拦截全部 Controller，解析权限码交 PermissionService 判定（判定结果进程内缓存，授权变更即时失效） |
| `common/config/ControllerLogAspect`                                  | 操作日志切面：自动记录所有非 GET 请求（密码脱敏、超长截断）                                                 |
| `common/config/RepeatSubmitAspect`                                   | 防重复提交切面实现                                                                                          |
| `common/config/CorsConfig`                                           | CORS 过滤器：来源白名单（配置化）+ 允许凭证（Session 需要）                                                 |
| `common/config/XssConfig`                                            | Jackson 反序列化中和脚本标签（XSS 防御）                                                                    |
| `common/config/WebConfig`                                            | 注册 JwtInterceptor 及其白名单                                                                              |
| `common/config/WebSocketConfig`                                      | 注册 WebSocket 服务端点                                                                                     |
| `common/config/Knife4jConfig`                                        | 接口文档（/doc.html）                                                                                       |
| `common/Result`                                                      | 统一返回体 `{code, msg, data}`                                                                              |
| `common/exception/CustomException`、`GlobalExceptionHandler`         | 业务异常与全局异常处理（统一错误码返回）                                                                    |
| `common/enums/ResultCodeEnum`、`RoleEnum`、`SegmentEnum`、`WeekEnum` | 错误码 / 角色 / 上课大节 / 星期 枚举                                                                        |
| `common/constants`（Constants）                                      | token 请求头名等常量                                                                                        |

### 2.2 Controller 层（HTTP 入口，继承 `CrudController` 获得 7 个标准端点）

| 文件                                                               | 职责                                                                                                        |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| `CrudController`                                                   | 通用增删改查基类：分页查询 / 详情 / 新增 / 修改 / 删除 / 批量删除 / 查询全部                                |
| `WebController`                                                    | 登录（验证码校验 + 登录保护 + 登录日志）、注册、改密码、主题/语言偏好查询与设置                             |
| `AdminController` / `TeacherController` / `StudentController`      | 三类账号管理；Student 额外提供 Excel 导入 / 导出 / 模板下载                                                 |
| `CollegeController` / `SpecialityController` / `ClassesController` | 学院 / 专业 / 班级（基础档案链）                                                                            |
| `CourseController`                                                 | 课程信息；额外提供 `GET /course/recommend` 协同过滤课程推荐                                                 |
| `ChoiceController`                                                 | 我的选课：退课、我的课表（含时间冲突校验见 Service）                                                        |
| `ScoreController`                                                  | 成绩录入与分布统计、Excel 导出                                                                              |
| `AttendanceController`                                             | 考勤登记与占比统计                                                                                          |
| `HomeworkController`                                               | 作业提交 / 批改（变更后 WebSocket 推送对方）                                                                |
| `ApplyController`                                                  | 请假申请 / 审核（审核结果 WebSocket 定点推送学生）                                                          |
| `CommentController`                                                | 网上评教（重复评教校验）                                                                                    |
| `NoticeController` / `ExamplanController` / `RoomplanController`   | 教务通知（新增全员 WebSocket 广播）/ 考试安排 / 教室安排                                                    |
| `DashboardController`                                              | 数据大屏聚合统计：一次返回指标卡、成绩分布、考勤占比、学院人数、选课热度、职称结构、登录趋势                |
| `FileController`                                                   | 文件上传（扩展名白名单 + MD5 去重命名）/ 下载（路径穿越防护 + 按扩展名 MIME）/ 删除                         |
| `LogController`                                                    | 操作 / 登录日志查询与删除                                                                                   |
| `PermissionController`                                             | RBAC：权限点与角色查询、角色授权保存（保存后失效权限缓存）                                                  |
| `WarningController`                                                | 学业预警：`GET /warning/list` 预警列表（按角色隔离）、`POST /warning/notify/{studentId}` WebSocket 推送提醒 |

### 2.3 Service 层（业务逻辑）

| 文件                                                                                                                                  | 职责                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --- | ---- | --- | ---- | -------------------------------------------------------------------------------------------------------- |
| `BaseService`                                                                                                                         | 三类账号通用服务：登录（BCrypt 校验 + 连续失败锁定计数）、注册、修改资料（行级 + 字段级防越权、密码加密）、修改密码、重置密码（ADMIN 专用且不可重置自己）、头像引用清理                                     |
| `CrudService`                                                                                                                         | 业务实体通用服务：增删改查分页 + 数据隔离钩子 `applyDataScope`（selectAll/selectPage 统一经由，防止绕过分页接口拿全量数据）                                                                                 |
| `PermissionService`                                                                                                                   | RBAC 核心：权限判定（进程内缓存，授权保存后失效）、当前用户权限码集合、角色授权整体保存                                                                                                                     |
| `LoginProtectService`                                                                                                                 | 登录保护：连续失败 5 次锁定 10 分钟（错误码 4008）                                                                                                                                                          |
| `RecommendService`                                                                                                                    | 算法 A：基于物品的协同过滤课程推荐。以选课记录构建"学生×课程"矩阵，课程间余弦相似度 `sim(A,B)=                                                                                                              | U(A)∩U(B) | /√( | U(A) | ·   | U(B) | )`，候选课得分=与已选课程相似度之和；冷启动（无选课/零相似）降级为按选课人数的热门推荐；已选课程自动排除 |
| `WarningService`                                                                                                                      | 算法 B：学业预警。风险指数 = 不及格占比×40 + 平均分差距×30 + 异常考勤率×30（缺勤计 1、迟到/早退计 0.5），≥60 高风险 / ≥30 中风险 / ≥10 低风险；数据范围按角色隔离（管理员全部、教师本人任课学生、学生本人） |
| `FileService`                                                                                                                         | 头像引用清理：资料更新导致头像变化时，统计三张账号表引用数，无人引用的旧文件删除（含路径穿越防护、中文名解码）                                                                                              |
| `ApplyService` / `HomeworkService`                                                                                                    | 请假审核 / 作业批改业务，变更后经 WebSocket 定点推送                                                                                                                                                        |
| `ChoiceService`                                                                                                                       | 选课核心：学分限定校验、上课时间冲突检测（同时间段已选则拒绝）、退课、我的课表组装                                                                                                                          |
| `ScoreService` / `AttendanceService` / `CommentService`                                                                               | 成绩（含学生维度数据隔离）/ 考勤（同时间段重复登记校验）/ 评教（重复评教校验）                                                                                                                              |
| `CourseService` / `CollegeService` / `SpecialityService` / `ClassesService` / `ExamplanService` / `RoomplanService` / `NoticeService` | 课程 / 学院 / 专业 / 班级 / 考试安排 / 教室安排 / 教务通知（新增广播）业务                                                                                                                                  |
| `LogService`                                                                                                                          | 操作 / 登录日志查询与批量删除                                                                                                                                                                               |

### 2.4 支撑层

| 文件 / 目录                                      | 职责                                                                                                                                           |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `mapper/`（20 个接口）+ `resources/mapper/*.xml` | MyBatis 数据访问：BaseMapper（账号通用）/ CrudMapper（业务通用）声明方法，XML 提供全部 SQL（含二级索引与动态条件）                             |
| `entity/`                                        | `Account`（账号公共字段，password 序列化 WRITE_ONLY 防哈希泄露）与 Admin/Teacher/Student 及各业务实体；`entity/excel/` 为 Excel 导入导出行模型 |
| `utils/TokenUtils`                               | JWT 签发与校验（密钥、过期小时数可配置/环境变量覆盖）；`getCurrentUser` 从请求头独立解析当前用户                                               |
| `utils/PasswordUtils`                            | BCrypt 加密与校验；存量明文密码首次登录自动升级                                                                                                |
| `websocket/NoticeWebSocketServer`                | WebSocket 服务端：连接时校验 token，按"用户ID-角色"登记在线连接，提供定点推送与全员广播，支持心跳                                              |
| `task/LogCleanupTask`                            | 定时任务：每天凌晨 2 点清理 90 天前的操作/登录日志                                                                                             |

### 2.5 资源与配置

| 文件                                  | 职责                                                                                |
| ------------------------------------- | ----------------------------------------------------------------------------------- |
| `application.yml`                     | 端口、数据源（Hikari）、multipart 上限（20MB）、CORS 白名单、JWT、MyBatis、日志级别 |
| `resources/mapper/*.xml`              | 各表 SQL（动态条件、IN 批量删除、二级索引对应查询）                                 |
| `sql/xm_educational_manager-full.sql` | 全量种子：表结构 + 索引 + 精简演示数据 + RBAC 授权（日志表仅结构）                  |
| `files/`                              | 上传文件存储目录（演示头像 5 个与账号一一对应）                                     |

---

## 三、Web 管理端文件地图（vue/src/）

### 3.1 应用骨架

| 文件                       | 职责                                                                                                                                                                                         |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `main.ts`                  | 入口：Element Plus 全量引入、Element 暗色变量、全局样式、图标注册、Pinia、i18n、路由、v-permission 指令、主题驱动初始化                                                                      |
| `App.vue`                  | 根组件：全局尺寸/灰度配置；启动 WebSocket 实时通知（登录后自动连接）                                                                                                                         |
| `router/router-index.ts`   | 路由表：按"信息公告/教学管理/教育管理/用户管理/系统管理"分组的全部页面路由；`meta.roles` 角色白名单、`meta.permission` 权限码、登录守卫三重校验（登录态 → 角色 → 权限码，失败跳 403/登录页） |
| `stores/user.ts`           | 用户状态：用户对象（含 token/角色/权限码），localStorage `xm-user` 持久化                                                                                                                    |
| `utils/request.ts`         | axios 封装：baseURL 取 `VITE_BASE_URL`（/api）、自动携带 token 头、401 统一清除登录态并跳登录页                                                                                              |
| `utils/file.ts`            | 文件地址归一化：历史绝对地址/相对地址统一转 `/api/files/` 代理路径                                                                                                                           |
| `directives/permission.ts` | `v-permission` 按钮级权限指令（对照权限码集合）                                                                                                                                              |

### 3.2 布局与通用组件

| 文件                                     | 职责                                                                                                                                      |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `views/Manager.vue`                      | 管理端布局壳：侧边栏分组菜单（按角色与权限码过滤）+ 顶部工具栏（面包屑/主题/语言/全屏/头像下拉）+ 多标签页 + 内容区；窄屏侧边栏自动变抽屉 |
| `components/CrudTable.vue`               | 通用表格组件（分页、操作列、自定义列插槽），与 useCrud 配套                                                                               |
| `components/useUser.ts`                  | 用户状态组合式访问入口（包装 stores/user）                                                                                                |
| `components/Form`、`Search`、`Dialog` 等 | 模板内置二次封装组件（表单/搜索/弹窗等，按需使用）                                                                                        |

### 3.3 组合式函数（composables/）

| 文件                                                | 职责                                                                                             |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `useCrud.ts`                                        | 通用 CRUD 逻辑：分页加载、删除确认、表单校验规则                                                 |
| `usePermission.ts`                                  | 登录后拉取 `/permission/my` 权限码并持久化                                                       |
| `useTheme.ts` / `useLocale.ts`                      | 主题三档（light/dark/跟随系统）/ 中英切换；偏好存数据库并与后端双向同步（防抖推送 + 拉取防回环） |
| `useNoticeSocket.ts`                                | WebSocket 实时通知客户端：登录自动连接、断线 10 秒重连、收到推送弹右上角通知、退出自动断开       |
| `useDownload.ts` / `useEChart.ts` / `useOptions.ts` | 文件下载 / ECharts 实例封装 / 下拉选项加载                                                       |

### 3.4 业务页面（views/）

| 页面                                                | 说明                                                              |
| --------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| `manager/Home.vue`                                  | 登录后首页：欢迎语、教务通知/考试安排预览、考勤占比图、成绩分布图 |
| `manager/Notice.vue` 等信息公告组                   | 教务通知 / 考试安排 / 教室安排（长文本展开查看）                  |
| `manager/College.vue` 等档案组                      | 学院 / 专业 / 班级管理（级联归属）                                |
| `manager/Course.vue`                                | 课程管理（教师视角仅本人课程）                                    |
| `manager/Choice.vue`                                | 我的选课（学生：退课/评教；含"为你推荐"协同过滤卡片）             |
| `manager/Curriculum.vue`                            | 我的课表（学生）                                                  |
| `manager/Score.vue`                                 | 成绩管理（教师录入，学生查看）                                    |
| `manager/Comment.vue`                               | 网上评教                                                          |
| `manager/Apply.vue`                                 | 请假申请（学生申请/撤销，管理员审核，实时推送结果）               |
| `manager/Homework.vue`                              | 作业提交（含附件上传）与批改                                      |
| `manager/Attendance.vue`                            | 考勤信息                                                          |
| `manager/Admin.vue` / `Teacher.vue` / `Student.vue` | 三类账号管理（含 Excel 导入导出、重置密码、头像）                 |
| `manager/OperLog.vue` / `LoginLog.vue`              | 操作 / 登录日志                                                   |
| `manager/Permission.vue`                            | RBAC 授权设置（在线勾选即时生效）                                 |
| `manager/AdminPerson.vue` 等三个 Person 页          | 各角色个人信息维护（含头像上传，成功即同步右上角）                |
| `manager/Password.vue`                              | 修改密码                                                          |
| `views/Manager.vue`                                 | 布局壳（见上）；`views/Dashboard.vue`                             | 数据大屏 |
| `views/Login.vue` / `Register.vue`                  | 登录 / 注册                                                       |
| `views/manager/Warning.vue`                         | 学业预警页：风险列表（按角色隔离）+ 推送提醒按钮                  |

---

## 四、App 小程序端文件地图（BISHE-mobile/src/）

| 文件 / 目录                                                     | 职责                                                                                                                          |
| --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `main.js` / `App.vue`                                           | 入口与全局配置（Pinia、i18n、主题类挂载）                                                                                     |
| `pages.json` / `manifest.json`                                  | 页面注册（含分包 preloadRule）、TabBar、小程序 AppID                                                                          |
| `utils/config.js`                                               | 后端地址（读取 .env 的 VITE_API_BASE_URL；头部附真机联调排错清单）                                                            |
| `utils/request.js`                                              | uni.request 封装：token 头、验证码 Session Cookie 手动维护、401 统一处理、`resolveFileUrl` 文件地址归一化、`getData` 简化取数 |
| `utils/websocket.js`                                            | 实时通知：推送 toast + 首页角标、心跳保活、断线指数退避重连（兼容各端 connectSocket 返回形态）                                |
| `stores/user.js`                                                | 用户状态（localStorage `xm-user` 持久化）                                                                                     |
| `composables/useCrud.js`                                        | 通用分页列表逻辑（与 Web 端 useCrud 语义对齐）                                                                                |
| `composables/useTheme.ts` / `useLocale.ts` / `usePermission.js` | 主题三档（含原生导航栏/tabBar 联动）/ 中英切换 / 权限码拉取                                                                   |
| `styles/theme.scss`                                             | CSS 变量主题 + xm-\* 通用组件类（卡片/按钮/标签/宫格/骨架屏/弹层，设计规范对齐高星项目）                                      |
| `pages/`（17 页）+ `pages-admin/`（6 页分包）                   | 登录注册 / 首页（宫格 + 通知 + 统计 + 推荐卡）/ 我的（头像跨端同步）+ 按角色业务页                                            |
| `locales/` + `i18n/`                                            | 中英语言包（词条键与 Web 端一致）                                                                                             |
| `.env.development` / `.env.production`                          | 多环境后端地址（VITE_API_BASE_URL）                                                                                           |

---

## 五、核心业务流程

### 5.1 登录认证流程

```
登录页（图形验证码 ← GET /captcha，Session 绑定）
  → POST /login {账号, 密码, 验证码, 角色}
     ├ 验证码校验（错误 → 402）
     ├ 登录保护（连续失败 5 次锁 10 分钟 → 4008，全部记登录日志）
     ├ BCrypt 密码比对（存量明文自动升级哈希）
     └ 成功：签发 JWT → 返回用户对象（含 token）
前端：token/角色/权限码写入 Pinia + localStorage
之后每个请求自动携带自定义头 token → JwtInterceptor 校验
```

### 5.2 RBAC 权限流程（三层联动）

```
登录成功 → GET /permission/my 拉取当前角色权限码集合 → 前端持久化
  ├ 路由层：router meta.roles + meta.permission 守卫校验（越权 → 403）
  ├ 菜单层：侧边栏按权限码/角色过滤显隐
  └ 按钮层：v-permission 指令控制操作按钮
每次接口请求：PermissionAspect 按数据库授权做服务端判定（ADMIN 放行，判定缓存化，授权保存即时失效）
```

### 5.3 以「课程」为中心的业务主链

```
学院 → 专业 → 班级 → 学生（基础档案链，数据大屏"学院人数"依赖此链）
教师 ──任教──> 课程（course.teacher_id，教师端只看本人课程）
选课（choice：学生×课程×教师）── 枢纽 ──┬→ 我的课表
                                      ├→ 成绩录入/查看
                                      ├→ 考勤登记/查看
                                      ├→ 作业提交/批改
                                      └→ 网上评教
请假独立成链：学生提交 → 管理员审核 → WebSocket 实时推送结果
```

### 5.4 实时通知流程（WebSocket）

```
登录成功 → 前端连接 ws://后端/ws/notice/{token}（连接时校验 token）
触发点：成绩发布/请假审核 → 定点推学生；提交作业 → 推教师；发布教务通知 → 全员广播
Web 端：ElNotification 右上角通知；App 端：toast + 「首页」未读角标（进入首页清零）
断线：自动重连（Web 10 秒间隔 / App 指数退避 + 心跳保活）
```

### 5.5 算法流程

**课程推荐（基于物品的协同过滤，RecommendService）**

```
选课记录 → 「学生×课程」0/1 矩阵
课程相似度：sim(A,B) = |U(A)∩U(B)| / √(|U(A)|·|U(B)|)
候选课得分 = Σ 与各已选课程的相似度
→ 取得分最高的 N 门（已选自动排除，附推荐理由）
冷启动（无选课/零相似）→ 降级为按选课人数的热门推荐
```

**学业预警（多指标加权评分，WarningService）**

```
风险指数(0~100) = 不及格占比×40 + 平均分差距×30 + 异常考勤率×30
  （异常考勤：缺勤计 1，迟到/早退计 0.5）
等级：≥60 高风险 / ≥30 中风险 / ≥10 低风险 / <10 正常
数据范围：管理员全部 / 教师本人任课学生 / 学生本人
触发动作：预警页查看 + WebSocket 定点推送提醒（App 端 toast + 角标）
```

### 5.6 文件上传下载流程

```
上传：前端直传 /files/upload（token + 权限 file:upload）
  → 扩展名白名单（图片/文档/压缩包 20 种）→ 内容 MD5 → 存储名 md5.ext（同内容去重）
  → 落盘 启动目录/files/ → 返回 /api/files/<md5>.<ext> 相对地址入库
下载：GET /files/{flag}（路径穿越防护 + 按扩展名返回 MIME）
清理：资料更新换头像时统计三表引用数，无人引用的旧文件自动删除
```

### 5.7 偏好多端同步流程

```
主题（light/dark/system）与语言（zh-CN/en-US）存账号表：
登录后 → GET /theme、/locale 拉取覆盖本地 → 修改后防抖 PUT 推送
Web 端同步 Element 暗色与自定义变量；App 端同步页面变量与原生导航/tabBar 配色
```

---

## 六、安全设计要点

| 层   | 措施                                                                                                      |
| ---- | --------------------------------------------------------------------------------------------------------- |
| 认证 | JWT（密钥/时效可配置）、BCrypt 密码哈希（明文自动升级）、图形验证码、连续失败锁定                         |
| 鉴权 | RBAC 三层（路由/菜单/按钮）+ 服务端切面逐请求判定 + token 自定义头                                        |
| 输入 | MyBatis 全 `#{}` 预编译（禁 `${}`、禁 allowMultiQueries）、XSS 反序列化中和、上传扩展名白名单 + 20MB 上限 |
| 文件 | MD5 重命名、下载路径穿越防护、按扩展名返回 MIME                                                           |
| 越权 | 行级（非管理员仅本人）+ 字段级（自助更新白名单）双层防护                                                  |
| 审计 | 操作日志（AOP 自动、密码脱敏）+ 登录日志（成功/失败），90 天自动清理                                      |

---

## 七、相关文档

- [数据库 ER 图](./数据库ER图.md)：21 张表实体关系（核心业务 + RBAC + 日志）与设计要点
- 根目录 `README.md`：部署说明（Docker/手动）、内置账号、安全加固清单、常见问题
